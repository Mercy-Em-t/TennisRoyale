{
  "name": "tennis-royale-functions",
  "private": true,
  "description": "Firebase Cloud Functions — Tennis Royale Tournament Platform",
  "version": "1.0.0",
  "scripts": {
    "build":  "tsc",
    "watch":  "tsc --watch",
    "serve":  "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs":   "firebase functions:log"
  },
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin":     "^12.0.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "typescript":           "^5.4.0",
    "@types/node":          "^20.0.0"
  }
}
